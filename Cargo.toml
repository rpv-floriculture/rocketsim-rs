[package]
name = "rocketsim"
version = "0.1.0"
edition = "2021"
authors = [ "ash lea <example@thisismyactual.email>" ]

[dependencies]
glam = "0.22.0"
libc = "0.2.139"

[build-dependencies]
cc = "1.0.78"

[features]
lto = []
fat-lto = []
